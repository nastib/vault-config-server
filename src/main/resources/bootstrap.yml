server:
  port: 8887
spring:
  application:
    name: vault-config-server

  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/IdeaProjects/spring-cloud-config-vault/vault-config-server #https://github.com/nastib/vault-config-server.git 
